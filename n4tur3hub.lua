// ==UserScript==
// @name         BypassTools
// @namespace    https://bypass.tools
// @version      3.0.2
// @description  Advanced AdLink bypass powered by BypassTools (Based on F.E.A.R. by Woozie)
// @author       BypassTools & Woozie (Original F.E.A.R.)
// @license      MIT
// @match        *://linkvertise.com/*
// @match        *://loot-link.com/*
// @match        *://loot-links.com/*
// @match        *://lootdest.com/*
// @match        *://lootdest.org/*
// @match        *://lootlinks.co/*
// @match        *://lootlinks.com/*
// @match        *://daughablelea.com/*
// @match        *://tonordersitye.com/*
// @match        *://cuty.io/*
// @match        *://www.cuty.io/*
// @match        *://work.ink/*
// @match        *://cety.app/*
// @match        *://www.cety.app/*
// @match        *://lockr.so/*
// @match        *://mega-guy.com/*
// @match        *://best-links.org/*
// @match        *://megaspremium.com/*
// @match        *://direct-links.net/*
// @match        *://risquemega.com/*
// @match        *://onlyfriends.club/*
// @match        *://onepiecered.co/*
// @match        *://multileaks.com/*
// @match        *://luvsquad-links.com/*
// @match        *://luvsquad-links.cmo/*
// @match        *://free-leaks.com/*
// @match        *://goldmega.online/*
// @match        *://realiukzemydre.com/*
// @match        *://kmendation.com/*
// @match        *://onlyshare.info/*
// @match        *://leakplugs.com/*
// @match        *://nswfbox.com/*
// @match        *://eofmukindwo.com/*
// @match        *://op-packs.com/*
// @match        *://thedetective.online/*
// @match        *://forlinkmeg.com/*
// @match        *://thegoatpack.org/*
// @match        *://of-leaks.xyz/*
// @match        *://links.spacebin.in/*
// @match        *://supermeg.com/*
// @match        *://onlyfanessereloaded.com/*
// @match        *://fanzleaks.com/*
// @match        *://cemendemons.com/*
// @match        *://premiummegaz.com/*
// @match        *://thepremium.online/*
// @match        *://pasteebins.com/*
// @match        *://baddiezcentral.com/*
// @match        *://megazone.website/*
// @match        *://leak-pragmatic.com/*
// @match        *://megadumpz.com/*
// @match        *://thhaven.net/*
// @match        *://meg4fans.com/*
// @match        *://depravityweb.co/*
// @match        *://discordlink.cc/*
// @match        *://megashub.co/*
// @match        *://streamergirls.org/*
// @match        *://your-leaks.com/*
// @match        *://onlylinksmegas.xyz/*
// @match        *://pypy.spacebin.in/*
// @match        *://pypy.in/*
// @match        *://beast.net.in/*
// @match        *://hook.beast.net.in/*
// @match        *://all-fans.online/*
// @match        *://fansmega.com/*
// @match        *://worldpacks.co/*
// @match        *://night-hub.online/*
// @match        *://dailyofleaks.com/*
// @match        *://pkofs.com/*
// @match        *://offree90.com/*
// @match        *://megadropz.com/*
// @match        *://onlyfunlink.com/*
// @match        *://direct-links.org/*
// @match        *://leaks4you.com/*
// @match        *://onlylinksmegas.com/*
// @match        *://direct-link.net/*
// @match        *://downbadleaks.com/*
// @match        *://onlyfanscloud.com/*
// @match        *://missleakz.com/*
// @match        *://leakszone.online/*
// @match        *://links-loot.com/*
// @match        *://holyfanslinks.com/*
// @match        *://utopianleaks.com/*
// @match        *://megavip.store/*
// @match        *://drlinker.com/*
// @match        *://baddiesheaven.com/*
// @match        *://of4lm-links.com/*
// @match        *://holedonly.store/*
// @match        *://free-content.pro/*
// @match        *://milky-center.com/*
// @match        *://megaofs.com/*
// @match        *://bleleadersto.com/*
// @match        *://link-target.org/*
// @match        *://mymegalinks.com/*
// @match        *://heroslut.com/*
// @match        *://vip-linknetwork.com/*
// @match        *://birdbiss.com/*
// @match        *://loot-labs.com/*
// @match        *://lootlabs.com/*
// @match        *://link-hub.net/*
// @match        *://locconnect.com/*
// @match        *://premiumstashdrop.com/*
// @match        *://lootdest.info/*
// @match        *://of-area.com/*
// @match        *://link-target.net/*
// @match        *://megalnk.com/*
// @match        *://lootlink.org/*
// @match        *://nsfwcherry.com/*
// @match        *://cherrypacks.online/*
// @match        *://hotstars-leaks.com/*
// @match        *://onlymega.co/*
// @match        *://hanimeturks.com/*
// @match        *://dailyleakz.com/*
// @match        *://content-hub.club/*
// @match        *://dailyadultmegas.com/*
// @match        *://oui-chu.com/*
// @match        *://megalinks.one/*
// @match        *://starleakz.com/*
// @match        *://babeslink.click/*
// @match        *://ofgirls3x.com/*
// @match        *://pnp-drops.me/*
// @match        *://nsfw-paradise.club/*
// @match        *://key-access.co/*
// @match        *://leaksmix.com/*
// @match        *://sensual-leaks.com/*
// @match        *://ofhub-leaks.com/*
// @match        *://onlyforfan.online/*
// @match        *://sweetjuice-mega.com/*
// @match        *://crip-hub.com/*
// @match        *://leaksfreeday.com/*
// @match        *://thotshaven.online/*
// @match        *://mega-leaked.com/*
// @match        *://beautifulgirls.social/*
// @match        *://tavernleaks.com/*
// @match        *://nudeleaksteenz.com/*
// @match        *://slutywet.com/*
// @match        *://leakutopia.site/*
// @match        *://secret-packs.com/*
// @match        *://attiktok22.com/*
// @match        *://pancakes-leaks.com/*
// @match        *://diamond-leaks.com/*
// @match        *://lewd-leaks.com/*
// @match        *://ftbleaks.net/*
// @match        *://thepytheaven.org/*
// @match        *://of-region.online/*
// @match        *://admiregirls-byme.com/*
// @match        *://mzehoney12.com/*
// @match        *://herplace.online/*
// @match        *://megaplugleaks.com/*
// @match        *://of-kingdom.com/*
// @match        *://onlyfansmegafolder.com/*
// @match        *://adfoc.us/*
// @match        *://boost.ink/*
// @match        *://mboost.me/*
// @match        *://bst.gg/*
// @match        *://booo.st/*
// @match        *://mendationforc.info/*
// @match        *://paster.so/*
// @match        *://paster.gg/*
// @match        *://rekonise.com/*
// @match        *://social-unlock.com/*
// @match        *://www.social-unlock.com/*
// @match        *://socialwolvez.com/*
// @match        *://sub2get.com/*
// @match        *://www.sub2get.com/*
// @match        *://sub2unlock.com/*
// @match        *://sub2unlock.io/*
// @match        *://sub2unlock.net/*
// @match        *://sub2unlock.online/*
// @match        *://sub2unlock.top/*
// @match        *://sub4unlock.pro/*
// @match        *://sub4unlock.com/*
// @match        *://sub4unlock.io/*
// @match        *://subfinal.com/*
// @match        *://unlocknow.net/*
// @match        *://v.gd/*
// @match        *://workink.net/*
// @match        *://r.work.ink/*
// @match        *://workink.one/*
// @match        *://workink.me/*
// @match        *://ytsubme.com/*
// @match        *://www.ytsubme.com/*
// @match        *://esohasl.net/*
// @match        *://rbscripts.net/*
// @match        *://link.rbscripts.net/*
// @match        *://bypass.tools/bypass?url=*
// @match        *://rip.linkvertise.lol/workink?url=*
// @match        *://*.*.*/*?referrer=*
// @match        *://bypass.tools/?url=*
// @match        *://*.*.*/easxlol*
// @exclude      *://tria.ge/*
// @exclude      *://*.google.*/*
// @exclude      *://work.ink/token/*


// @grant        GM_setValue
// @grant        GM_getValue
// @grant        GM_deleteValue
// @grant        GM_xmlhttpRequest
// @grant        GM_getResourceText
// @grant        unsafeWindow
// @grant        GM_addStyle
// @connect      bypass.tools
// @connect      work-direct.ink
// @require      https://cdnjs.cloudflare.com/ajax/libs/jquery/3.7.1/jquery.min.js
// @resource     NOTYF_CSS https://cdn.jsdelivr.net/npm/notyf@3/notyf.min.css
// @require      https://cdn.jsdelivr.net/npm/notyf@3/notyf.min.js
// @downloadURL  https://bypass.tools/bypasstools-fear.js
// @updateURL    https://bypass.tools/bypasstools-fear.js
// @homepageURL  https://bypass.tools
// @supportURL   https://bypass.tools/support
// @run-at       document-start
// ==/UserScript==

(function () {
    'use strict';
    if (window.top !== window.self) { return }

    const BT_ORIGINAL_FETCH = unsafeWindow.fetch;
    const BT_RECOVERY = {
        active: false,
        lastErrorAt: 0,
        count: 0
    };

    function btSafeToString(err) {
        try {
            if (!err) return 'Unknown error';
            if (typeof err === 'string') return err;
            if (err && typeof err.message === 'string') return err.message;
            return String(err);
        } catch {
            return 'Unknown error';
        }
    }

    function btResetRuntimeState() {
        try { unsafeWindow.EASX_Running = false; } catch { }
        try { unsafeWindow.easxModuleRunning = false; } catch { }
        try { unsafeWindow.wokeresponse = null; } catch { }
        try { unsafeWindow.fetch = BT_ORIGINAL_FETCH; } catch { }
        try { if (unsafeWindow.easxOverlayInterval) { clearInterval(unsafeWindow.easxOverlayInterval); unsafeWindow.easxOverlayInterval = null; } } catch { }
    }

    function btShowRecoveryOverlay(message) {
        if (BT_RECOVERY.active) return;
        BT_RECOVERY.active = true;

        try { document.getElementById('bypasstools-recovery')?.remove(); } catch { }

        const root = document.createElement('div');
        root.id = 'bypasstools-recovery';
        root.style.cssText = [
            'position:fixed',
            'inset:0',
            'z-index:2147483647',
            'background:rgba(0,0,0,0.92)',
            'display:flex',
            'align-items:center',
            'justify-content:center',
            'font-family:Inter,system-ui,-apple-system,Segoe UI,Roboto,Arial,sans-serif',
            'padding:20px'
        ].join(';');

        const card = document.createElement('div');
        card.style.cssText = [
            'width:min(520px, 92vw)',
            'background:#18181b',
            'border:1px solid #27272a',
            'border-radius:16px',
            'padding:28px',
            'box-shadow:0 0 60px rgba(139,92,246,0.18)',
            'color:#fff',
            'text-align:center'
        ].join(';');

        const title = document.createElement('div');
        title.textContent = 'BypassTools Recovery';
        title.style.cssText = 'font-weight:800;font-size:22px;margin-bottom:10px;background:linear-gradient(135deg,#fff,#a78bfa);-webkit-background-clip:text;-webkit-text-fill-color:transparent;';

        const body = document.createElement('div');
        body.textContent = message;
        body.style.cssText = 'color:#a1a1aa;font-size:14px;line-height:1.5;margin-bottom:18px;white-space:pre-wrap;word-break:break-word;';

        const btn = document.createElement('button');
        btn.textContent = 'Retry Bypass ->';
        btn.style.cssText = 'width:100%;padding:14px 18px;border-radius:12px;border:none;cursor:pointer;font-weight:800;font-size:15px;color:#fff;background:linear-gradient(135deg,#8b5cf6,#7c3aed);box-shadow:0 6px 22px rgba(139,92,246,0.35);';

        const btn2 = document.createElement('button');
        btn2.textContent = 'Close';
        btn2.style.cssText = 'width:100%;padding:12px 18px;border-radius:12px;margin-top:10px;cursor:pointer;font-weight:700;font-size:14px;color:#e4e4e7;background:transparent;border:1px solid #3f3f46;';

        btn.onclick = () => {
            try { root.remove(); } catch { }
            BT_RECOVERY.active = false;
            btResetRuntimeState();
            try {
                if (typeof unsafeWindow.easxRetry === 'function') {
                    unsafeWindow.easxRetry();
                    return;
                }
            } catch { }
            try { location.reload(); } catch { }
        };
        btn2.onclick = () => {
            try { root.remove(); } catch { }
            BT_RECOVERY.active = false;
            btResetRuntimeState();
        };

        card.appendChild(title);
        card.appendChild(body);
        card.appendChild(btn);
        card.appendChild(btn2);
        root.appendChild(card);
        document.documentElement.appendChild(root);
    }

    function btHandleFatalError(err, source) {
        const now = Date.now();
        if (now - BT_RECOVERY.lastErrorAt < 800) return;
        BT_RECOVERY.lastErrorAt = now;
        BT_RECOVERY.count += 1;

        const msg = btSafeToString(err);
        console.error('[BypassTools] Fatal error:', source, err);
        btResetRuntimeState();

        // Show recovery UI instead of silently dying.
        btShowRecoveryOverlay(`An error occurred and bypass was paused to prevent a stuck state.\n\nSource: ${source}\nError: ${msg}`);
    }

    // Catch async crashes so the script doesn't appear to "stop working" until a browser restart.
    unsafeWindow.addEventListener('error', (e) => {
        const err = e?.error || e?.message;
        if (!err) return;
        // "Script error." = cross-origin script; browser strips all detail. Not our code.
        if (typeof e.message === 'string' && e.message === 'Script error.') return;
        // Ignore errors originating from Work.ink's own bundled scripts or 3rd-party SDKs
        const src = e?.filename || '';
        if (src.includes('_app/immutable') || src.includes('stripe.com')) return;
        // Ignore if the stack traces back to Work.ink internals
        const stack = (e?.error?.stack) || '';
        if (stack.includes('_app/immutable') || stack.includes('scheduler.') || stack.includes('singletons.')) return;
        btHandleFatalError(err, 'window.error');
    }, true);

    unsafeWindow.addEventListener('unhandledrejection', (e) => {
        const reason = e?.reason;
        if (!reason) return;
        // Ignore unhandled rejections from Work.ink's own SvelteKit/Svelte runtime
        const stack = (reason?.stack) || '';
        const msg = String(reason?.message || reason || '');
        if (stack.includes('_app/immutable') || stack.includes('scheduler.') || stack.includes('singletons.')) return;
        if (msg.includes('component initialization') || msg.includes('Function called outside')) return;
        btHandleFatalError(reason, 'unhandledrejection');
    });

    // GM_xmlhttpRequest-based fetch polyfill for CORS
    function gmFetch(url, options = {}) {
        return new Promise((resolve, reject) => {
            GM_xmlhttpRequest({
                method: options.method || 'GET',
                url: url,
                headers: options.headers || {},
                data: options.body,
                onload: function (response) {
                    resolve({
                        ok: response.status >= 200 && response.status < 300,
                        status: response.status,
                        statusText: response.statusText,
                        text: () => Promise.resolve(response.responseText),
                        json: () => Promise.resolve(JSON.parse(response.responseText))
                    });
                },
                onerror: function (error) {
                    console.error('[BypassTools] gmFetch Error:', error);
                    reject(error);
                }
            });
        });
    }

    // Expose gmFetch on window so module inner functions can always reach it
    window.gmFetch = gmFetch;


    // ================================
    // LOOTLABS WS PROXY HOOK
    // ================================
    // Hook the page's own WebSocket so we capture the r: token from its socket.
    // The LootLabs server only sends r: to the FIRST connected socket (the page SDK).
    // A duplicate socket we open never receives it, so we intercept the page's instead.
    (function btHookLootLabsWS() {
        try {
            const _OrigWS = unsafeWindow.WebSocket;
            unsafeWindow.WebSocket = new Proxy(_OrigWS, {
                construct(Target, args) {
                    const ws = Reflect.construct(Target, args);
                    const wsUrl = typeof args[0] === 'string' ? args[0] : '';
                    if (wsUrl.includes('/c?uid=') && wsUrl.includes('&session_id=')) {
                        console.log('[BypassTools] LootLabs WebSocket detected, hooking for r: token...');
                        ws.addEventListener('message', function (ev) {
                            if (typeof ev.data !== 'string') return;
                            const m = ev.data.match(/(?:^|\b)r:\s*([^\s]+)/i);
                            if (!m || !m[1]) return;
                            const tok = m[1].replace(/[\u0000-\u001F\u007F]/g, '').trim();
                            if (!tok) return;
                            console.log('[BypassTools] WS hook: r: token captured from page socket');
                            unsafeWindow._btLootlabsToken = tok;
                            unsafeWindow.dispatchEvent(new CustomEvent('BT_LOOTLABS_TOKEN', { detail: tok }));
                        });
                    }
                    return ws;
                }
            });
        } catch (e) {
            console.warn('[BypassTools] WebSocket hook setup failed:', e);
        }
    })();

    // ================================
    // EARLY URL CAPTURE
    // ================================

    // Capture the original URL as early as possible before any modifications
    (function captureOriginalUrl() {
        if (!unsafeWindow.easxEarlyCapture) {
            const currentUrl = location.href;

            // Skip if this is clearly a resource or processed URL
            if (!currentUrl.includes('favicon.ico') && !currentUrl.includes('.css') && !currentUrl.includes('.js')) {
                unsafeWindow.easxEarlyCapture = currentUrl;

                // Store in sessionStorage immediately
                try {
                    sessionStorage.setItem('easx_early_capture', currentUrl);
                    console.log('[BypassTools] Early capture of URL:', currentUrl);
                } catch (e) { }
            }
        }
    })();

    if (location.hostname.includes("loot-link") || location.hostname.includes("lootdest") || location.hostname.includes("lootlink")) { setInterval(() => { document.querySelector(".fc-monetization-dialog-container")?.remove() }, 2500) }
    if (location.href.startsWith("http://bypass.tools/workink?url=") ||
        location.href.startsWith("https://bypass.tools/workink?url=")) {
        unsafeWindow.EASX_Running = true;
        return;
    }
    // work.ink token page just shows the key — no bypass needed
    if (location.hostname === 'work.ink' && location.pathname.startsWith('/token/')) return;
    // Global retry function for website retry buttons
    unsafeWindow.easxRetry = function () {
        console.log('[BypassTools] Retry function called from website');

        // Get the original URL for retry
        let retryUrl = null;

        // Special handling if we're on a bypass page - extract the original URL from params
        if (location.pathname === '/bypass' && location.search.includes('url=')) {
            const urlParam = new URLSearchParams(location.search).get('url');
            if (urlParam) {
                retryUrl = decodeURIComponent(urlParam);
                console.log('[BypassTools] Extracted URL from bypass page params:', retryUrl);
            }
        }

        // Priority 1: Use early capture if available (only if not a bypass URL)
        if (!retryUrl && unsafeWindow.easxEarlyCapture &&
            !unsafeWindow.easxEarlyCapture.includes('favicon.ico') &&
            !unsafeWindow.easxEarlyCapture.includes('/bypass?url=')) {
            retryUrl = unsafeWindow.easxEarlyCapture;
        }

        // Priority 2: Try sessionStorage early capture (only if not a bypass URL)
        if (!retryUrl) {
            try {
                const earlyCapture = sessionStorage.getItem('easx_early_capture');
                if (earlyCapture && !earlyCapture.includes('favicon.ico') &&
                    !earlyCapture.includes('/bypass?url=')) {
                    retryUrl = earlyCapture;
                }
            } catch (e) { }
        }

        // Priority 3: SessionStorage original URL (only if not a bypass URL)
        if (!retryUrl) {
            try {
                const storedOriginal = sessionStorage.getItem('easx_original_url');
                if (storedOriginal && !storedOriginal.includes('favicon.ico') &&
                    !storedOriginal.includes('/bypass?url=')) {
                    retryUrl = storedOriginal;
                }
            } catch (e) { }
        }

        // Priority 4: unsafeWindow stored URLs (only if not a bypass URL)
        if (!retryUrl) {
            if (unsafeWindow.easxOriginalUrl && !unsafeWindow.easxOriginalUrl.includes('favicon.ico') &&
                !unsafeWindow.easxOriginalUrl.includes('/bypass?url=')) {
                retryUrl = unsafeWindow.easxOriginalUrl;
            } else if (unsafeWindow.easxStoredOriginal && !unsafeWindow.easxStoredOriginal.includes('favicon.ico') &&
                !unsafeWindow.easxStoredOriginal.includes('/bypass?url=')) {
                retryUrl = unsafeWindow.easxStoredOriginal;
            }
        }

        // Final fallback
        if (!retryUrl) {
            retryUrl = 'https://linkvertise.com/';
        }

        // Ensure we have a valid URL and clean it
        try {
            // Clean the URL - remove any double protocols
            retryUrl = retryUrl.replace(/^https:\/\/https:\/\//, 'https://');
            retryUrl = retryUrl.replace(/^http:\/\/https:\/\//, 'https://');
            retryUrl = retryUrl.replace(/^https:\/\/http:\/\//, 'http://');

            new URL(retryUrl);
        } catch (e) {
            retryUrl = 'https://linkvertise.com/';
        }

        console.log('[BypassTools] Global retry with URL:', retryUrl);

        // Restart the bypass process
        const CONFIG = {
            wait: unsafeWindow.easxSecureMode ? 25 : 1,
            apiBase: 'bypass.tools'
        };
        const newBypassUrl = `https://${CONFIG.apiBase}/wait?url=${encodeURIComponent(retryUrl)}&wait=${CONFIG.wait}`;

        console.log('[BypassTools] Redirecting to:', newBypassUrl);
        window.location.href = newBypassUrl;
    };

    // Enhanced retry button detection
    function findRetryButton() {
        const selectors = [
            'button[onclick*="retry"]',
            'button[onclick*="Retry"]',
            'button:contains("Try Again")',
            'button:contains("Retry")',
            'button:contains("Try again")',
            'button:contains("RETRY")',
            'button:contains("TRY AGAIN")',
            '#retry-btn',
            '.retry-btn',
            '.try-again',
            '[data-action="retry"]',
            'button[title*="retry"]',
            'button[title*="Try again"]'
        ];

        for (const selector of selectors) {
            const button = document.querySelector(selector);
            if (button) return button;
        }

        // Check for buttons with retry-related text
        const buttons = document.querySelectorAll('button, .btn, [role="button"]');
        for (const button of buttons) {
            const text = button.textContent?.toLowerCase() || '';
            const title = button.title?.toLowerCase() || '';
            const ariaLabel = button.getAttribute('aria-label')?.toLowerCase() || '';

            if (text.includes('retry') || text.includes('try again') ||
                title.includes('retry') || title.includes('try again') ||
                ariaLabel.includes('retry') || ariaLabel.includes('try again')) {
                return button;
            }
        }

        return null;
    }

    const CONFIG = {
        wait: 1, // Initial delay (in seconds) before starting the bypass process.
        site: 'bypass.tools/wait?url=',
        timeout: 6000000, // Maximum time (in milliseconds) to wait for the bypass completion check.
        interval: 500, // Interval (in milliseconds) between checks for bypass completion.
        SecureMode: true,
        //^^ Enables SecureMode, which increases the wait time to 30 seconds and adds a manual click-button prompt to proceed
        //^^ Enhancing stealth and reducing detectability of the bypass by up to 100%.
        WorkInk: true, // Enable Work.ink client-side bypass
        LootLabs: true, // Enable LootLabs client-side bypass

        UVersion: "V2.4-EASX",
        apiBase: 'https://bypass.tools',
        bypassPage: 'https://bypass.tools/bypass?url='
    };

    // Adjust wait time if SecureMode is true
    if (CONFIG.SecureMode) {
        CONFIG.wait = 25;
        unsafeWindow.easxSecureMode = true; // Store for global retry function
    }

    // ==========================================
    // UNIFIED UI SYSTEM (Global Scope)
    // ==========================================
    function createUnifiedOverlay(config) {
        // Prevent duplicate overlays
        if (document.getElementById('bypasstools-overlay')) return;

        // 1. Inject Styles
        const style = document.createElement('style');
        style.textContent = `
            @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Roboto+Mono:wght@400;500&display=swap');

            :root {
                --bg-color: #000000;
                --card-bg: #18181b;
                --accent: #8b5cf6;
                --accent-dim: rgba(139, 92, 246, 0.1);
                --border: #3f3f46;
                --text-primary: #ffffff;
                --text-secondary: #a1a1aa;
                --font-main: 'Inter', sans-serif;
                --font-mono: 'Roboto Mono', monospace;
            }

            #bypasstools-overlay {
                position: fixed; top: 0; left: 0; width: 100vw; height: 100vh;
                background: var(--bg-color); color: var(--text-primary);
                font-family: var(--font-main); display: flex; flex-direction: column;
                align-items: center; justify-content: center; z-index: 2147483647;
                overflow-y: auto; padding: 20px; box-sizing: border-box;
                transition: background 0.25s ease;
            }

            #bypasstools-overlay.bt-minimized {
                background: transparent; pointer-events: none;
                display: flex; align-items: flex-end; justify-content: flex-end;
                padding: 0;
            }

            #bypasstools-overlay.bt-minimized #bypasstools-card {
                display: none;
            }

            /* Minimized pill — always visible in corner */
            #bt-mini-pill {
                display: none; pointer-events: auto;
                position: fixed; bottom: 16px; right: 16px; z-index: 2147483647;
                background: #18181b; border: 1px solid #3f3f46;
                border-radius: 999px; padding: 10px 18px 10px 12px;
                font-family: var(--font-main); font-size: 1rem; font-weight: 700;
                color: #a78bfa; user-select: none;
                display: flex; align-items: center; gap: 10px;
                box-shadow: 0 4px 20px rgba(0,0,0,0.7);
                transition: box-shadow 0.2s;
            }
            #bt-mini-pill .bt-mini-dot {
                width: 10px; height: 10px; border-radius: 50%; background: #8b5cf6;
                animation: bt-pulse-dot 2s infinite; flex-shrink: 0;
            }
            @keyframes bt-pulse-dot { 0%,100%{opacity:.5;transform:scale(1)} 50%{opacity:1;transform:scale(1.3)} }

            #bypasstools-card {
                background: var(--card-bg); border: 1px solid var(--border);
                border-radius: 16px; padding: 40px; width: 100%; max-width: 600px;
                box-shadow: 0 0 50px rgba(139, 92, 246, 0.15);
                display: flex; flex-direction: column; align-items: center; text-align: center;
                animation: bt-fade-in 0.5s ease-out;
                position: relative;
            }

            /* ... Title/Subtitle ... */
            h1#bypasstools-title {
                font-size: 2.8rem; font-weight: 700; margin: 0 0 0.5rem 0;
                background: linear-gradient(135deg, #fff, #a78bfa);
                -webkit-background-clip: text; -webkit-text-fill-color: transparent;
                letter-spacing: -0.02em;
            }

            p#bypasstools-subtitle {
                font-size: 1.1rem; color: var(--text-secondary); margin: 0 0 2rem 0;
            }

            /* Enhanced Status Area - The new "Processing Thing" */
            #bypasstools-status-text {
                 animation: bt-pulse 2s infinite; font-weight: 700; color: var(--text-primary); font-size: 1.4rem;
                 margin-bottom: 1rem;
            }
            .bt-status-container { width: 100%; margin: 20px 0; }
            
            #bypasstools-api-ref {
                margin-top: 30px; font-size: 0.85rem; color: #52525b;
            }
            
            /* Status/Loading Animation - Central & Big */
            .bt-loading-bar-c { width: 100%; height: 10px; background: #27272a; border-radius: 10px; overflow: hidden; margin-top: 15px; }
            .bt-loading-bar-i { height: 100%; background: var(--accent); width: 0%; transition: width 0.3s ease; box-shadow: 0 0 15px var(--accent); }
            
            .bt-status-detail { 
                font-size: 1.1rem; color: #e4e4e7; margin-top: 15px; 
                display: flex; justify-content: space-between; font-weight: 600; align-items: center; 
                padding: 0 5px;
            }
            .bt-spinner { 
                width: 24px; height: 24px; 
                border: 3px solid var(--border); border-top: 3px solid var(--accent); 
                border-radius: 50%; animation: bt-spin 1s linear infinite; 
                display: inline-block; margin-right: 10px; vertical-align: text-bottom; 
            }

            /* Reverted Button Style (Violet) */
            .bypasstools-btn {
                background: linear-gradient(135deg, #8b5cf6, #7c3aed); 
                color: #fff; border: none;
                padding: 16px 32px; border-radius: 8px; font-weight: 700; font-size: 1.2rem;
                cursor: pointer; transition: all 0.2s ease;
                box-shadow: 0 4px 15px rgba(139, 92, 246, 0.4);
                width: 100%; margin-top: 30px; box-sizing: border-box;
                text-transform: uppercase; letter-spacing: 0.5px;
            }
            .bypasstools-btn:hover {
                background: linear-gradient(135deg, #a78bfa, #8b5cf6);
                transform: translateY(-2px);
                box-shadow: 0 6px 25px rgba(139, 92, 246, 0.6);
            }
            
            .bypasstools-btn:disabled {
                background: #27272a; color: #52525b; cursor: not-allowed;
                box-shadow: none; transform: none;
            }

            @keyframes bt-cursor-blink { 0%, 100% { opacity: 1; } 50% { opacity: 0; } }
            @keyframes bt-pulse { 0% { opacity: 0.6; } 50% { opacity: 1; text-shadow: 0 0 10px var(--accent); } 100% { opacity: 0.6; } }
            @keyframes bt-spin { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }

            @keyframes bt-fade-in { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: translateY(0); } }
            @media (max-width: 640px) {
                #bypasstools-card { padding: 20px; width: 95%; }
                h1#bypasstools-title { font-size: 2rem; }
            }
        `;
        document.head.appendChild(style);

        // GM_xmlhttpRequest Helper
        window.gmFetch = function (url, options = {}) {
            return new Promise((resolve, reject) => {
                GM_xmlhttpRequest({
                    method: options.method || 'GET',
                    url: url,
                    headers: options.headers || {},
                    data: options.body,
                    onload: (res) => {
                        resolve({
                            ok: res.status >= 200 && res.status < 300,
                            status: res.status,
                            statusText: res.statusText,
                            text: () => Promise.resolve(res.responseText),
                            json: () => Promise.resolve(JSON.parse(res.responseText))
                        });
                    },
                    onerror: (err) => reject(err)
                });
            });
        };
        // (style already appended above)

        // 2. Create DOM Structure
        const overlay = document.createElement('div');
        overlay.id = 'bypasstools-overlay';

        const card = document.createElement('div');
        card.id = 'bypasstools-card';

        // Minimized corner pill
        const miniPill = document.createElement('div');
        miniPill.id = 'bt-mini-pill';
        miniPill.innerHTML = '<span class="bt-mini-dot"></span><span id="bt-mini-text">BypassTools – click to restore</span>';
        document.body.appendChild(miniPill);

        // Toggle logic
        let _btMinimized = false;
        function _btToggleMinimize() {
            _btMinimized = !_btMinimized;
            if (_btMinimized) {
                overlay.classList.add('bt-minimized');
                miniPill.style.display = 'flex';
            } else {
                overlay.classList.remove('bt-minimized');
                miniPill.style.display = 'none';
            }
        }

        const title = document.createElement('h1');
        title.id = 'bypasstools-title';
        title.textContent = config.title || 'BypassTools';

        const subtitle = document.createElement('p');
        subtitle.id = 'bypasstools-subtitle';
        subtitle.textContent = config.subtitle || 'Advanced Bypass System';

        // Information/Status Area (Legacy Support)
        const infoDiv = document.createElement('div');
        infoDiv.style.width = "100%";
        infoDiv.innerHTML = `<div style="color:var(--text-secondary); margin-bottom:10px; font-size:1rem;">${config.description || 'Processing request...'}</div>`;

        // Add new animated status container if ID matches (for Work.ink) or explicitly requested
        if ((config.statusElementId === 'fear-status' || config.useAnimatedStatus) && config.statusElementId !== null) {
            const statusContainer = document.createElement('div');
            statusContainer.id = config.statusElementId || 'bt-status-c';
            statusContainer.className = 'bt-status-container';
            statusContainer.innerHTML = `
                <div class="bt-loading-bar-c"><div class="bt-loading-bar-i" style="width: 5%;"></div></div>
                <div class="bt-status-detail">
                    <span><div class="bt-spinner"></div>Initializing...</span>
                    <span>0%</span>
                </div>
             `;
            infoDiv.appendChild(statusContainer);
        } else if (config.statusElementId) {
            // Legacy simple status
            const statusP = document.createElement('p');
            statusP.id = config.statusElementId;
            statusP.className = 'bt-status-container';
            statusP.innerHTML = `<span id="bypasstools-status-text">CONNECTED TO SERVER</span>`;
            infoDiv.appendChild(statusP);
        }

        card.appendChild(title);
        card.appendChild(subtitle);

        // Console Removed as requested

        card.appendChild(infoDiv);

        // Action Button
        const btn = document.createElement('button');
        btn.className = 'bypasstools-btn';
        btn.id = config.redirectBtnId || 'bypasstools-action-btn';
        btn.textContent = 'Go to Destination →';
        btn.style.display = 'none'; // Hidden by default

        if (config.onButtonClick) {
            btn.onclick = config.onButtonClick;
        }

        card.appendChild(btn);

        // API Reference
        const apiRef = document.createElement('div');
        apiRef.id = 'bypasstools-api-ref';
        apiRef.textContent = 'Powered by Original API & BypassTools v2.1';
        card.appendChild(apiRef);

        overlay.appendChild(card);
        document.body.appendChild(overlay);

        return {
            overlay,
            card,
            infoDiv,
            consoleDiv: config.showConsole ? (card.querySelector('#' + (config.consoleElementId || 'bypasstools-console'))) : null,
            consoleBody: null,
            button: btn,
            minimize: (pillText) => {
                if (!_btMinimized) _btToggleMinimize();
                const t = document.getElementById('bt-mini-text');
                if (t && pillText) t.textContent = pillText;
            },
            restore: (pillText) => {
                if (_btMinimized) _btToggleMinimize();
                const t = document.getElementById('bt-mini-text');
                if (t && pillText) t.textContent = pillText;
            }
        };
    }


    // ==========================================
    // WORK.INK MODULE — SvelteKit Application-Layer Bypass
    // ==========================================
    if (CONFIG.WorkInk && location.hostname === 'work.ink') {
        // Token page: bypass already complete, just shows the key
        if (location.pathname.startsWith('/token/')) return;

        const WI_API_BASE = 'https://wik22.bypass.tools';
        const WI_API_KEY  = '12345678';

        function _wiLog(msg) { console.log('[BypassTools/WI]', msg); }

        // Name resolvers
        const WI_NAME_MAP = {
            onLinkInfo: ['onLinkInfo'], onLinkDestination: ['onLinkDestination'],
            onProxyDetected: ['onProxyDetected'], onMonocleFailed: ['onMonocleFailed']
        };
        function _wiResolveName(obj, candidates) {
            for (const name of candidates) { if (typeof obj[name] === 'function') return { fn: obj[name], name }; }
            return { fn: null, name: null };
        }
        function _wiResolveWriteFn(obj) {
            if (typeof obj.mlebvbmbklrobamrhuqxspr === 'function') return { fn: obj.mlebvbmbklrobamrhuqxspr, name: 'mlebvbmbklrobamrhuqxspr' };
            const names = new Set([...Object.keys(obj), ...Object.getOwnPropertyNames(obj)]);
            try { const p = Object.getPrototypeOf(obj); if (p && p !== Object.prototype) Object.getOwnPropertyNames(p).forEach(n => names.add(n)); } catch {}
            for (const name of names) {
                try { const v = obj[name]; if (typeof v === 'function' && v.length === 2 && name !== 'constructor') return { fn: v, name }; } catch {}
            }
            return { fn: null, name: null };
        }
        function _wiDecodeJwtExp(jwt) {
            try { const b64 = jwt.split('.')[1].replace(/-/g, '+').replace(/_/g, '/'); const p = b64 + '='.repeat((4 - b64.length % 4) % 4); return JSON.parse(atob(p)).exp || 0; } catch { return 0; }
        }

        const WI_PKT = { SOCIAL_STARTED: 'c_social_started', ADBLOCKER_DETECTED: 'c_adblocker_detected', TURNSTILE_RESPONSE: 'c_turnstile_response', PING: 'c_ping' };

        // State
        let _wiSessionCtrl, _wiLinkInfo, _wiSendMessage, _wiOnLinkInfo, _wiOnLinkDest, _wiChallengeSolved = false, _wiOverlayCtrl = null;

        // ── Executor ──────────────────────────────────────────────────────────
        async function _wiExecute(instructions) {
            for (const instr of instructions) {
                if (instr.delay > 0) await new Promise(r => setTimeout(r, instr.delay));
                switch (instr.type) {
                    case 'social':
                        if (_wiSendMessage && _wiSessionCtrl)
                            try { _wiSendMessage.call(_wiSessionCtrl, WI_PKT.SOCIAL_STARTED, { url: instr.url }); } catch {}
                        break;
                    case 'offer_event': {
                        const offer = (_wiSessionCtrl?.monetizations ?? []).find(m => m.id === instr.offerId);
                        if (offer) try { offer.sendMessage?.({ event: instr.event }); } catch {}
                        break;
                    }
                    case 'http_request': {
                        const opts = { method: instr.method || 'GET', mode: 'no-cors' };
                        if (instr.body) { opts.headers = { 'Content-Type': 'application/json' }; opts.body = JSON.stringify(instr.body); }
                        try { unsafeWindow.fetch(instr.url, opts).catch(() => {}); } catch {}
                        break;
                    }
                    default: _wiLog('Unknown instruction type: ' + instr.type);
                }
            }
        }

        function _wiWrapMonetizations() {
            const mons = _wiSessionCtrl?.monetizations;
            if (!Array.isArray(mons) || mons.length === 0) return;
            for (const m of mons) {
                if (typeof m.onMessage !== 'function' || m.__btRelayWrapped) continue;
                m.__btRelayWrapped = true;
                const orig = m.onMessage.bind(m);
                m.onMessage = function(msg) {
                    gmFetch(`${WI_API_BASE}/offer-event`, {
                        method: 'POST', headers: { 'Content-Type': 'application/json', 'X-API-Key': WI_API_KEY },
                        body: JSON.stringify({ offerId: m.id, offerName: m.name, event: msg })
                    }).then(r => {
                        if (!r || !r.ok) return;
                        r.json().then(json => {
                            if (Array.isArray(json.instructions) && json.instructions.length > 0)
                                _wiExecute(json.instructions);
                        }).catch(() => {});
                    }).catch(() => {});
                    return orig(msg);
                };
            }
        }

        // ── UI helpers (piggybacking createUnifiedOverlay IDs) ─────────────────
        function _wiUpdateStatus(pct, msg) {
            const bar = document.querySelector('#fear-status .bt-loading-bar-i');
            if (bar) bar.style.width = Math.max(5, Math.min(100, pct)) + '%';
            const det = document.querySelector('#fear-status .bt-status-detail');
            if (det) det.innerHTML = `<span>${msg}</span><span>${Math.round(pct)}%</span>`;
        }
        function _wiShowSuccess(url) {
            // Restore fullscreen overlay now that verification is complete
            if (_wiOverlayCtrl) _wiOverlayCtrl.restore('✓ Verification complete!');
            _wiUpdateStatus(100, 'Bypass Complete — click to continue');
            const bar = document.querySelector('#fear-status .bt-loading-bar-i');
            if (bar) { bar.style.background = '#8b5cf6'; bar.style.boxShadow = '0 0 20px #8b5cf6'; }
            const btn = document.querySelector('.bypasstools-btn');
            if (btn) btn.style.display = 'block';
            unsafeWindow.wokeresponse = url;
        }

        // ── Bypass solver ─────────────────────────────────────────────────────
        async function _wiSolve() {
            if (!_wiChallengeSolved || !_wiLinkInfo) return;
            _wiUpdateStatus(20, 'Captcha solved — contacting server...');
            const payload = {
                socials: (_wiLinkInfo.socials ?? []).map(s => ({ url: s.url })),
                offers:  (_wiSessionCtrl?.monetizations ?? []).map(m => ({ id: m.id, name: m.name }))
            };
            try {
                const resp = await gmFetch(`${WI_API_BASE}/bypass`, {
                    method: 'POST', headers: { 'Content-Type': 'application/json', 'X-API-Key': WI_API_KEY },
                    body: JSON.stringify(payload)
                });
                if (!resp || !resp.ok) throw new Error('HTTP ' + resp?.status);
                const json = await resp.json();
                _wiUpdateStatus(60, 'Running bypass steps...');
                await _wiExecute(json.instructions || []);
            } catch (e) { _wiLog('Bypass API error: ' + e.message); _wiUpdateStatus(100, 'Bypass server error — refresh'); }
        }

        // ── Session controller proxy ───────────────────────────────────────────
        function _wiSetupProxy() {
            const sendMsg = _wiResolveWriteFn(_wiSessionCtrl);
            const lInfo   = _wiResolveName(_wiSessionCtrl, WI_NAME_MAP.onLinkInfo);
            const lDest   = _wiResolveName(_wiSessionCtrl, WI_NAME_MAP.onLinkDestination);
            const lProxy  = _wiResolveName(_wiSessionCtrl, WI_NAME_MAP.onProxyDetected);
            const lMono   = _wiResolveName(_wiSessionCtrl, WI_NAME_MAP.onMonocleFailed);
            _wiSendMessage = sendMsg.fn; _wiOnLinkInfo = lInfo.fn; _wiOnLinkDest = lDest.fn;

            const sendMsgProxy = function(...args) {
                const type = args[0];
                if (type !== WI_PKT.PING) _wiLog('sendMessage: ' + type);
                if (type === WI_PKT.ADBLOCKER_DETECTED) return;
                if (type === 'c_env_check') { try { return _wiSendMessage.apply(this, ['c_env_check', { flags: [] }]); } catch {} return; }
                if (type === WI_PKT.TURNSTILE_RESPONSE) {
                    let ret; try { ret = _wiSendMessage.apply(this, args); } catch (e) { _wiLog('sendMessage err: ' + e.message); }
                    _wiChallengeSolved = true;
                    // Update pill text to show processing state (still minimized)
                    const pillText = document.getElementById('bt-mini-text');
                    if (pillText) pillText.textContent = '⏳ Processing bypass...';
                    _wiSolve(); return ret;
                }
                try { return _wiSendMessage.apply(this, args); } catch (e) { _wiLog('sendMessage err: ' + e.message); }
            };
            const onLinkInfoProxy = function(...args) {
                _wiLog('onLinkInfo fired'); _wiLinkInfo = args[0];
                try { Object.defineProperty(_wiLinkInfo, 'isAdblockEnabled', { get() { return false; }, set() {}, configurable: true }); } catch {}
                _wiWrapMonetizations();
                // Auto-minimize so user can see and solve the captcha
                if (_wiOverlayCtrl) _wiOverlayCtrl.minimize('⚠ Solve the captcha to continue');
                _wiSolve(); return _wiOnLinkInfo?.apply(this, args);
            };
            const onLinkDestProxy = function(...args) {
                const url = args[0]?.url; if (url) _wiShowSuccess(url); return _wiOnLinkDest?.apply(this, args);
            };
            const _def = (name, get, setFn) => {
                if (!name) return;
                try { Object.defineProperty(_wiSessionCtrl, name, { get() { return get; }, set(v) { setFn(v); }, configurable: true, enumerable: true }); }
                catch (e) { _wiLog('defineProperty failed: ' + name + ' ' + e.message); }
            };
            _def(sendMsg.name, sendMsgProxy,   v => { _wiSendMessage = v; });
            _def(lInfo.name,   onLinkInfoProxy, v => { _wiOnLinkInfo  = v; });
            _def(lDest.name,   onLinkDestProxy, v => { _wiOnLinkDest  = v; });
            _def(lProxy.name,  () => {},        () => {});
            _def(lMono.name,   () => {},        () => {});
            _wiLog('Session controller proxied: ' + [sendMsg.name, lInfo.name, lDest.name].join(', '));
        }

        // ── Node wrappers ─────────────────────────────────────────────────────
        function _wiCheckForCtrl(target, prop, value, receiver) {
            if (value && typeof value === 'object' && _wiResolveWriteFn(value).fn &&
                _wiResolveName(value, WI_NAME_MAP.onLinkInfo).fn &&
                _wiResolveName(value, WI_NAME_MAP.onLinkDestination).fn && !_wiSessionCtrl) {
                _wiSessionCtrl = value; _wiLog('Session controller intercepted'); _wiSetupProxy();
            }
            return Reflect.set(target, prop, value, receiver);
        }
        function _wiPageCtxSet(target, prop, value, receiver) {
            if (prop === '1') return value;
            return Reflect.set(target, prop, value, receiver);
        }
        function _wiWrapNode(origNode, ctxSet) {
            return async (...args) => {
                const result = await origNode(...args);
                return new Proxy(result, {
                    get(t, p, r) {
                        if (p === 'component') return new Proxy(t.component, {
                            construct(tgt, a) {
                                const inst = Reflect.construct(tgt, a);
                                if (ctxSet) inst.$$.ctx = new Proxy(inst.$$.ctx, { set: ctxSet });
                                return inst;
                            }
                        });
                        return Reflect.get(t, p, r);
                    }
                });
            };
        }

        // ── SYNC: googletag stub, fetch shim, Promise.all hook ────────────────
        try { unsafeWindow.googletag = { cmd: [], _loaded_: true }; } catch {}
        const _wiOrigFetch = unsafeWindow.fetch;
        unsafeWindow.fetch = function(...args) {
            const url = typeof args[0] === 'string' ? args[0] : (args[0]?.url || '');
            if (url === 'https://js.stripe.com/v3/' && args[1]?.method === 'HEAD' && args[1]?.mode === 'no-cors')
                return Promise.resolve(new Response('', { status: 200 }));
            if (url === '/country.json')
                return Promise.resolve(new Response(JSON.stringify({ countryCode: 'US' }), { status: 200 }));
            return _wiOrigFetch.apply(this, args);
        };

        const _wiOrigPA = unsafeWindow.Promise.all.bind(unsafeWindow.Promise);
        let _wiPADone = false;
        unsafeWindow.Promise.all = async function(promises) {
            const resultPromise = _wiOrigPA(promises);
            if (_wiPADone) return resultPromise;
            let modules; try { modules = await resultPromise; } catch(e) { throw e; }
            if (_wiPADone) return modules;
            const [first] = modules;
            if (first && typeof first === 'object' && typeof first.start === 'function') {
                _wiPADone = true;
                unsafeWindow.Promise.all = _wiOrigPA;
                _wiLog('SvelteKit bundle intercepted');
                // Report bundle URL to server for version tracking (non-critical)
                try {
                    const entries = unsafeWindow.performance?.getEntriesByType?.('resource') ?? [];
                    const pageEntry = entries.find(e => /_page\.[a-f0-9]+\.js/.test(e.name));
                    if (pageEntry) {
                        gmFetch(`${WI_API_BASE}/report-bundle`, {
                            method: 'POST',
                            headers: { 'Content-Type': 'application/json', 'X-API-Key': WI_API_KEY },
                            body: JSON.stringify({ bundleUrl: pageEntry.name })
                        }).catch(() => {});
                    }
                } catch { /* non-critical */ }
                const origStart = first.start;
                const kitProxy = new Proxy(first, {
                    get(t, p, r) {
                        if (p !== 'start') return Reflect.get(t, p, r);
                        return async function(...args) {
                            let waited = 0;
                            while (!window._btWIKitPatchedStart && waited < 5000) {
                                await new Promise(r => setTimeout(r, 50)); waited += 50;
                            }
                            if (window._btWIKitPatchedStart) return window._btWIKitPatchedStart(...args);
                            return origStart.apply(t, args);
                        };
                    }
                });
                window._btWIKitIntercepted = { kit: first, origStart };
                if (Array.isArray(window._btWIKitReadyCallbacks))
                    window._btWIKitReadyCallbacks.forEach(cb => cb(window._btWIKitIntercepted));
                return [kitProxy, ...modules.slice(1)];
            }
            return modules;
        };

        // ── ASYNC: CF check, overlay, token, kit patch ────────────────────────
        (async () => {
            // Wait for head content, detect CF challenge
            await new Promise(resolve => {
                const iv = setInterval(() => { if (document.head?.textContent) { clearInterval(iv); resolve(); } }, 50);
            });
            const headText = (document.head.textContent || '').toLowerCase();
            if (headText.includes('just a mome') || headText.includes('just a second') || headText.includes('attention required')) {
                _wiLog('Cloudflare challenge page — halting');
                return;
            }

            // Show overlay once body is ready
            await new Promise(resolve => {
                if (document.body) return resolve();
                const biv = setInterval(() => { if (document.body) { clearInterval(biv); resolve(); } }, 50);
            });
            _wiOverlayCtrl = createUnifiedOverlay({
                title: 'BypassTools',
                subtitle: 'Advanced Work.ink Bypass System',
                description: 'Solving captcha & bypassing link logic...',
                statusElementId: 'fear-status',
                redirectBtnId: 'fear-redirect-btn',
                showConsole: false,
                useAnimatedStatus: true,
                onButtonClick: () => { if (unsafeWindow.wokeresponse) location.href = unsafeWindow.wokeresponse; }
            }) || null;
            // Start minimized — captcha must be solved first
            if (_wiOverlayCtrl) _wiOverlayCtrl.minimize('⚠ Solve the captcha to continue');

            // Token: wik22 → genAccount → localStorage
            const _wiTokenReady = (async () => {
                try {
                    let serverToken = null;
                    try {
                        const tr = await gmFetch(WI_API_BASE + '/token', { headers: { 'X-API-Key': WI_API_KEY } });
                        if (tr && tr.ok) { const tj = await tr.json(); serverToken = ((tj && (tj.token || tj.customerToken)) || '').trim() || null; }
                    } catch (_) {}
                    if (!serverToken) {
                        try {
                            const gr = await gmFetch('https://work-direct.ink/api/clientSides/workink/genAccount');
                            if (gr && gr.ok) { const gj = await gr.json(); const raw = (gj && (gj.niggaID || gj.customerToken || gj.token)) || ''; serverToken = String(raw).trim() || null; }
                        } catch (_) {}
                    }
                    const existing = localStorage.getItem('customerToken');
                    const now = Date.now() / 1000;
                    const token = [serverToken, existing].filter(Boolean).find(t => { const exp = _wiDecodeJwtExp(t); return exp === 0 || exp > now + 300; }) || null;
                    if (!token) { _wiLog('No valid customerToken — proceeding without'); return; }
                    localStorage.setItem('customerToken', token);
                    const exp = _wiDecodeJwtExp(token);
                    document.cookie = 'customerToken=' + encodeURIComponent(token) + '; path=/; domain=.work.ink' +
                        (exp ? '; expires=' + new Date(exp * 1000).toUTCString() : '; max-age=86400') + '; SameSite=None; Secure';
                    _wiLog('customerToken ready');
                } catch (e) { _wiLog('Token error: ' + e.message); }
            })();

            // Register kit patch callback
            const _doKitPatch = ({ kit, origStart }) => {
                window._btWIKitPatchedStart = async function(...startArgs) {
                    await _wiTokenReady;
                    _wiLog('Token step complete — patching nodes');
                    const appModule = startArgs[0], options = startArgs[2];
                    if (typeof appModule === 'object' && typeof appModule.nodes === 'object' &&
                        typeof options === 'object' && typeof options.node_ids === 'object') {
                        const pid = options.node_ids[0], aid = options.node_ids[1];
                        _wiLog('node_ids: ' + JSON.stringify(options.node_ids));
                        if (pid != null && appModule.nodes[pid]) appModule.nodes[pid] = _wiWrapNode(appModule.nodes[pid], _wiPageCtxSet);
                        if (aid != null && appModule.nodes[aid]) appModule.nodes[aid] = _wiWrapNode(appModule.nodes[aid], _wiCheckForCtrl);
                    }
                    return origStart.apply(kit, startArgs);
                };
                _wiLog('Kit patched — waiting for captcha...');
                _wiUpdateStatus(10, 'Waiting for captcha...');
            };

            if (window._btWIKitIntercepted) {
                _doKitPatch(window._btWIKitIntercepted);
            } else {
                if (!Array.isArray(window._btWIKitReadyCallbacks)) window._btWIKitReadyCallbacks = [];
                window._btWIKitReadyCallbacks.push(_doKitPatch);
            }
        })();
        return; // Stop further execution on this page
    }
    // ==========================================
    // LOOTLABS MODULE (BypassTools Rebranded)
    // ==========================================
    if (CONFIG.LootLabs && location.href.includes("/s?")) {
        if (!false) {
            !function () {
                "use strict"; const e = unsafeWindow.fetch;
                unsafeWindow.fetch = async function (t, n = {}) {
                    if ("string" != typeof t && !(t instanceof Request)) return e(t, n);
                    const o = t instanceof Request ? t.url : t;
                    if ("POST" !== (n.method || (t instanceof Request ? t.method : "GET")).toUpperCase() || !o.endsWith("/tc")) return e(t, n);
                    let s = n.body || (t instanceof Request ? await t.clone().text() : null);
                    if (!s) return e(t, n);
                    let i, c = !1;
                    try { const e = JSON.parse(s); e && "object" == typeof e && ("max_tasks" in e && (e.max_tasks = 1, e.num_of_tasks = "1", e.bl = [18,2,33,7,21,49], e.tier_id = "0", e.taboola_user_sync = "0", c = !0), i = JSON.stringify(e)) } catch (e) { }
                    if (c) { const s = { ...n, body: i, headers: { ...n.headers, "Content-Type": "application/json" } }; return e(t instanceof Request ? t : o, s) }
                    return e(t, n)
                }, console.log("[BypassTools] Intercepted fetch")
            }();

            let overlayCreated = !1;
            let requestCount = 0;

            function updateLootLabsStatus(requestsDone) {
                const statusEl = document.getElementById('fear-status');
                if (!statusEl) return;

                // 6 requests total
                let percent = Math.min(100, Math.max(10, (requestsDone / 6) * 100));

                const bar = statusEl.querySelector('.bt-loading-bar-i');
                const details = statusEl.querySelector('.bt-status-detail');

                if (bar) bar.style.width = percent + '%';
                if (details) {
                    details.innerHTML = `
                        <span><div class="bt-spinner"></div>Bypassing AdMaven/LootLabs...</span>
                        <span>Request ${requestsDone}/6</span>
                    `;
                }
            }

            function updateWKestCount() {
                updateLootLabsStatus(requestCount);
            }

            function createBypassOverlay() {
                if (overlayCreated) return;
                overlayCreated = !0;

                const ui = createUnifiedOverlay({
                    title: "BypassTools",
                    subtitle: "Bypassing LootLabs Client-Side...",
                    statusElementId: "fear-status",
                    redirectBtnId: "fear-redirect-btn",
                    showConsole: false,
                    useAnimatedStatus: true,
                    onButtonClick: () => {
                        console.log("[BypassTools] Redirecting to:", unsafeWindow.wokeresponse);
                        unsafeWindow.wokeresponse && unsafeWindow.open(unsafeWindow.wokeresponse);
                    }
                });

                // Update text content specific to LootLabs
                const barC = ui.infoDiv.querySelector('.bt-loading-bar-c');
                if (barC) {
                    // Customize initial state
                    updateLootLabsStatus(0);
                }

                // Initial Button State: HIDDEN & DISABLED
                if (ui.button) {
                    ui.button.textContent = "Processing...";
                    ui.button.disabled = true;
                    ui.button.style.display = "none"; // Hide until complete
                }

                // Z-Index Enforcer (kept from original)
                setInterval(() => {
                    const ov = document.getElementById("bypasstools-overlay");
                    if (ov) ov.style.zIndex = "2147483647";
                }, 500);
            }

            function showRedirectButton(e) {
                console.log("[BypassTools] Showing redirect button for:", e);
                unsafeWindow.wokeresponse = e;

                if (!document.getElementById("fear-redirect-btn")) {
                    createBypassOverlay();
                }

                const t = document.getElementById("fear-redirect-btn");
                if (t) {
                    t.disabled = false;
                    t.textContent = "Go to Destination →";
                    t.style.display = "block"; // SHOW BUTTON NOW

                    // Update Status to Complete
                    const statusEl = document.getElementById('fear-status');
                    if (statusEl) {
                        const bar = statusEl.querySelector('.bt-loading-bar-i');
                        const details = statusEl.querySelector('.bt-status-detail');
                        if (bar) { bar.style.width = '100%'; bar.style.background = '#8b5cf6'; bar.style.boxShadow = '0 0 20px #8b5cf6'; }
                        if (details) details.innerHTML = `<span style="color:#8b5cf6; font-weight:700;">✅ Bypass Complete</span><span>100%</span>`;
                    }

                    console.log("[BypassTools] Button enabled!");
                }
            }

            const originalFetch = unsafeWindow.fetch;
            unsafeWindow.fetch = async function (...e) {
                const [t] = e;
                if (("string" == typeof t ? t : t.url).includes("/tc")) try {
                    const t = await originalFetch(...e), n = await t.clone().json();
                    console.log("[BypassTools] Intercepted TC!");
                    setTimeout((() => { createBypassOverlay() }), 1500);

                    if (Array.isArray(n) && n.length > 0) {
                        let { urid: e, task_id: t, action_pixel_url: o, session_id: s } = n[0];
                        1 === n.length ? (e = n[0].urid, t = n[0].task_id) : (e = n.map((e => e.urid)).join(","), t = n.map((e => e.task_id)).join(","));

                        const i = parseInt(e.slice(-5)) % 3;
                        const c = "undefined" != typeof INCENTIVE_SERVER_DOMAIN ? INCENTIVE_SERVER_DOMAIN : "onsultingco.com";
                        const a = "undefined" != typeof INCENTIVE_SYNCER_DOMAIN ? INCENTIVE_SYNCER_DOMAIN : "nismscoldnesfspu.org";
                        const r = "undefined" != typeof KEY ? KEY : (unsafeWindow.conf_rew?.key || '');
                        const l = "undefined" != typeof TID ? TID : (unsafeWindow.conf_rew?.cd || '');
                        const d = location.href.includes("loot") ? "1" : "0";

                        let settled = false;

                        const handleToken = async (rawTok) => {
                            if (settled) return;
                            settled = true;
                            const tok = rawTok.replace(/[^\x20-\x7E]/g, '').trim();
                            if (!tok) return;
                            try {
                                requestCount = 6; updateWKestCount();

                                // Extract a destination URL from any server response shape
                                const extractPyl = (data) => {
                                    if (!data) return null;
                                    if (typeof data === 'string') {
                                        const s = data.trim();
                                        if (/^https?:\/\//i.test(s)) return s;
                                        return null;
                                    }
                                    const candidates = [
                                        data.pyl, data.url, data.destination, data.redirect,
                                        data.link, data.result, data?.data?.pyl, data?.data?.url
                                    ];
                                    for (const c of candidates) {
                                        if (!c) continue;
                                        if (typeof c === 'string' && /^https?:\/\//i.test(c.trim())) return c.trim();
                                        if (Array.isArray(c)) {
                                            for (const item of c) {
                                                if (typeof item === 'string' && /^https?:\/\//i.test(item.trim())) return item.trim();
                                            }
                                        }
                                    }
                                    return null;
                                };

                                const tryEndpoint = async (url) => {
                                    try {
                                        const resp = await gmFetch(url);
                                        if (!resp.ok) {
                                            console.warn('[BypassTools] LootLabs API HTTP', resp.status, url);
                                            return null;
                                        }
                                        const rawText = await resp.text();
                                        console.log('[BypassTools] LootLabs API response:', rawText.substring(0, 200));
                                        try {
                                            return extractPyl(JSON.parse(rawText));
                                        } catch {
                                            const s = rawText.trim();
                                            return /^https?:\/\//i.test(s) ? s : null;
                                        }
                                    } catch (_e) {
                                        console.warn('[BypassTools] LootLabs API call failed:', _e);
                                        return null;
                                    }
                                };

                                const base = CONFIG.apiBase;
                                const enc = encodeURIComponent;
                                // Try payl/pal param convention first (fear/trw.lat style)
                                let pyl = await tryEndpoint(`${base}/api/clientSides/lootlabs?payl=${enc(tok)}&pal=${enc(location.href)}`);
                                // Fallback: try r/u convention
                                if (!pyl) pyl = await tryEndpoint(`${base}/api/clientSides/lootlabs?r=${enc(tok)}&u=${enc(location.href)}`);

                                if (pyl) {
                                    showRedirectButton(pyl);
                                } else {
                                    console.error('[BypassTools] LootLabs solve: no destination from API. Token (first 30):', tok.substring(0, 30));
                                    const statusEl = document.getElementById('fear-status');
                                    if (statusEl) {
                                        const bar = statusEl.querySelector('.bt-loading-bar-i');
                                        const details = statusEl.querySelector('.bt-status-detail');
                                        if (bar) { bar.style.width = '100%'; bar.style.background = '#ef4444'; bar.style.boxShadow = '0 0 20px rgba(239,68,68,0.6)'; }
                                        if (details) details.innerHTML = `<span style="color:#fda4af;font-weight:700;">Bypass stalled — reload to retry</span><span>Error</span>`;
                                    }
                                }
                            } catch (err) {
                                console.error('[BypassTools] Token solve error:', err);
                            }
                        };

                        // Listen for token fired by the WS Proxy hook on the page's own socket
                        unsafeWindow.addEventListener('BT_LOOTLABS_TOKEN', function btTokenHandler(ev) {
                            unsafeWindow.removeEventListener('BT_LOOTLABS_TOKEN', btTokenHandler);
                            handleToken(ev.detail);
                        });
                        // In case the page's WS already fired before we got here
                        if (unsafeWindow._btLootlabsToken) {
                            const tok = unsafeWindow._btLootlabsToken;
                            unsafeWindow._btLootlabsToken = null;
                            handleToken(tok);
                        }

                        // Fallback: our own duplicate socket (may not get r: but try anyway)
                        const u = new WebSocket(`wss://${i}.${c}/c?uid=${e}&cat=${t}&key=${r}&session_id=${s}&is_loot=${d}&tid=${l}`);
                        let pingInterval;
                        u.onopen = () => {
                            pingInterval = setInterval(() => {
                                if (settled) { clearInterval(pingInterval); return; }
                                try { u.send("0"); } catch { }
                                requestCount = Math.min(6, requestCount + 1);
                                updateWKestCount();
                            }, 1e4);
                        };
                        u.onmessage = async ev => {
                            const payload = typeof ev.data === 'string' ? ev.data : String(ev.data || '');
                            const m = payload.match(/(?:^|\b)r:\s*(.+)$/i);
                            if (!m || !m[1]) return;
                            clearInterval(pingInterval);
                            handleToken(m[1]);
                        };
                        u.onclose = () => { clearInterval(pingInterval); };
                        u.onerror = () => { clearInterval(pingInterval); };
                        navigator.sendBeacon(`https://${i}.${c}/st?uid=${e}&cat=${t}`);
                        fetch(o);
                        fetch(`https://${a}/td?ac=auto_complete&urid=${e}&cat=${t}&tid=${l}`);
                    }
                    return t
                } catch (t) { return console.error("[BypassTools] Bypass error:", t), originalFetch(...e) }
                return originalFetch(...e)
            };
        }
        return;
    }

    // Adjust wait time if SecureMode is true
    if (CONFIG.SecureMode) {
        CONFIG.wait = 25;
        unsafeWindow.easxSecureMode = true; // Store for global retry function
    }

    // Global click listener for retry buttons
    document.addEventListener('click', function (e) {
        const target = e.target;
        const text = target.textContent?.toLowerCase() || '';
        const title = target.title?.toLowerCase() || '';
        const onclick = target.getAttribute('onclick') || '';

        // Check if this looks like a retry button
        if ((text.includes('retry') || text.includes('try again') ||
            title.includes('retry') || title.includes('try again') ||
            onclick.includes('retry')) &&
            (target.tagName === 'BUTTON' || target.classList.contains('btn') || target.getAttribute('role') === 'button')) {

            console.log('[BypassTools] Retry button click detected');

            // Check if we're on a bypass page that failed
            if (document.querySelector("#bypass-title")?.textContent === "BYPASS FAILED!" ||
                document.body.innerHTML.includes('bypass failed') ||
                document.body.innerHTML.includes('Bypass failed') ||
                document.body.innerHTML.includes('BYPASS FAILED')) {

                e.preventDefault();
                e.stopPropagation();

                console.log('[BypassTools] Intercepting retry button for BypassTools retry');
                unsafeWindow.easxRetry();
            }
        }
    }, true); // Use capture phase to ensure we catch it first

    const notyfCss = GM_getResourceText("NOTYF_CSS");
    GM_addStyle(notyfCss);
    const notyf = new Notyf({ duration: 5000 });
    notyf.information = function (message) {
        this.success({
            message: message,
            duration: 4000,
            dismissible: true,
            background: '#1E88E5',
            icon: '<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="white" viewBox="0 0 16 16"><path d="M8 16A8 8 0 1 0 8 0a8 8 0 0 0 0 16zm.93-9.412l-1 4.705c-.07.34.029.533.304.533.194 0 .487-.07.686-.246l-.088.416c-.287.346-.92.598-1.465.598-.703 0-1.002-.422-.808-1.319l.738-3.468c.064-.302.156-.506.258-.678l-.803-.303c-.21.105-.402.26-.523.465l-.728 3.424c-.07.34-.029.533-.304.533-.194 0-.487.07-.686.246l.088-.416c.287-.346.92-.598 1.465-.598.703 0 1.002.422.808 1.319l-.738 3.468c-.064.302-.156.506-.258.678l.803.303c.21-.105.402-.26.523-.465l.728-3.424c.07-.34.029-.533.304-.533.194 0 .487.07.686.246l-.088.416c-.287.346-.92.598-1.465.598-.703 0-1.002-.422-.808-1.319l.738-3.468c-.064.302-.156.506-.258-.678l-.803-.303c-.21.105-.402.26-.523.465z"/></svg>'
        });
    };

    const Log = {
        i: (msg) => console.log(`[BypassTools] ${msg}`),
        e: (msg) => console.error(`[BypassTools ERROR] ${msg}`),
        w: (msg) => console.warn(`[BypassTools WARN] ${msg}`),
    };

    async function sleep(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async function findHref() {
        const start = Date.now();
        while (Date.now() - start < CONFIG.timeout) {
            await sleep(CONFIG.interval);
            try {
                if (document.querySelector("#bypass-title")?.textContent === "BYPASS DONE" && unsafeWindow.easxSuccess) {
                    return unsafeWindow.easxResult;
                } else if (document.querySelector("#bypass-title")?.textContent === "BYPASS FAILED!") {
                    return false;
                }
            } catch (err) {
                console.log(`Find href error: ${err}`);
            }
        }
        return false;
    }

    function isBot() {
        const t = document.title;
        return t.includes('Just a moment') || t.includes('Just a second') || document.body.innerHTML.includes('Are you human?');
    }

    function isIndexPage() {
        const url = location.href.toLowerCase();
        const pathname = location.pathname.toLowerCase();

        // Check if it's a root/index page
        if (pathname === '/' || pathname === '' || pathname === '/index' || pathname === '/index.html') {
            return true;
        }

        // Check for common index page patterns
        const indexPatterns = [
            /\/index\.html?$/,
            /\/home\.html?$/,
            /\/main\.html?$/,
            /\/$/, // Just domain with trailing slash
            /^https?:\/\/[^\/]+\/?$/ // Just domain with optional trailing slash
        ];

        for (const pattern of indexPatterns) {
            if (pattern.test(url)) {
                return true;
            }
        }

        // Check if URL has no path parameters (likely index)
        if (!url.includes('?') && !url.includes('#') && pathname.split('/').length <= 2) {
            return true;
        }

        return false;
    }

    async function main() {
        if (isBot()) {
            notyf.information('Complete the bot challenge to proceed');
            return;
        }

        // Don't show popup on index pages - nothing to bypass
        if (isIndexPage()) {
            Log.i('Index page detected, skipping bypass popup');
            return;
        }

        const p = new URLSearchParams(window.location.search);
        const ref = p.get('referrer');
        // Only activate the referrer relay on the bypass relay path (/favicon.ico).
        // Any other page that legitimately uses ?referrer= (payment gateways, OAuth,
        // analytics, etc.) should be left completely untouched.
        if (ref && location.pathname !== '/favicon.ico') {
            Log.i('?referrer= found but not on relay path — ignoring', location.pathname);
            return;
        }
        if (ref) {
            // Store the original URL for retry functionality (smart extraction)
            if (!unsafeWindow.easxOriginalUrl) {
                let originalUrl = null;

                // Priority 1: Use early capture if available
                if (unsafeWindow.easxEarlyCapture) {
                    originalUrl = unsafeWindow.easxEarlyCapture;
                }

                // Priority 2: Try sessionStorage early capture
                if (!originalUrl) {
                    try {
                        originalUrl = sessionStorage.getItem('easx_early_capture');
                    } catch (e) { }
                }

                // Priority 3: Current URL (cleaned)
                if (!originalUrl) {
                    originalUrl = location.href;

                    // Remove any hash parameters or processing markers
                    originalUrl = originalUrl.split('#')[0];
                    originalUrl = originalUrl.split('?hash=')[0];
                    originalUrl = originalUrl.split('&hash=')[0];

                    // Check if this looks like a bypass URL pattern and extract the real original
                    if (originalUrl.includes('bypass.vip') || originalUrl.includes('rip.linkvertise')) {
                        // Try to extract from referrer or URL params
                        const urlParams = new URLSearchParams(location.search);
                        if (urlParams.get('url')) {
                            originalUrl = decodeURIComponent(urlParams.get('url'));
                        } else if (document.referrer && !document.referrer.includes('bypass')) {
                            originalUrl = document.referrer;
                        }
                    }
                }

                // Avoid favicon.ico and other resource URLs
                if (!originalUrl || originalUrl.includes('favicon.ico') || originalUrl.includes('.css') || originalUrl.includes('.js')) {
                    // Fall back to the hostname
                    originalUrl = `https://${location.hostname}/`;
                }

                unsafeWindow.easxOriginalUrl = originalUrl;

                // Store in sessionStorage as backup
                try {
                    sessionStorage.setItem('easx_original_url', originalUrl);
                } catch (e) {
                    console.log('[BypassTools] Could not store in sessionStorage:', e);
                }

                console.log('[BypassTools] Stored original URL for retry:', originalUrl);
            }

            history.replaceState(null, null, '/' + ([1e7] + -1e3 + -4e3 + -8e3 + -1e11).replace(/[018]/g, c => (c ^ crypto.getRandomValues(new Uint8Array(1))[0] & 15 >> c / 4).toString(16)))
            setInterval(() => history.replaceState(null, null, '/' + ([1e7] + -1e3 + -4e3 + -8e3 + -1e11)
                .replace(/[018]/g, c => (c ^ crypto.getRandomValues(new Uint8Array(1))[0] & 15 >> c / 4).toString(16))), 1000);

            if (CONFIG.SecureMode) {
                unsafeWindow.dest = decodeURIComponent(ref);
                document.documentElement.innerHTML = `<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BypassTools - Destination Ready</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg-color: #000000;
            --card-bg: #18181b;
            --accent: #8b5cf6;
            --accent-hover: #7c3aed;
            --text-primary: #ffffff;
            --text-secondary: #a1a1aa;
        }

        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--bg-color);
            color: var(--text-primary);
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            overflow: hidden;
        }

        .container {
            background: var(--card-bg);
            border: 1px solid #27272a;
            border-radius: 20px;
            padding: 50px;
            text-align: center;
            box-shadow: 0 0 50px rgba(139, 92, 246, 0.15);
            max-width: 480px;
            width: 90%;
            animation: fadeIn 0.8s ease-out;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .status-badge {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            background: rgba(34, 197, 94, 0.1);
            color: #4ade80;
            padding: 8px 16px;
            border-radius: 20px;
            font-size: 0.9rem;
            font-weight: 600;
            margin-bottom: 25px;
            border: 1px solid rgba(34, 197, 94, 0.2);
        }

        h1.title {
            font-size: 2.2rem;
            font-weight: 800;
            margin: 0 0 15px;
            background: linear-gradient(135deg, #fff, #a78bfa);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        p.subtitle {
            color: var(--text-secondary);
            font-size: 1.05rem;
            line-height: 1.6;
            margin-bottom: 35px;
        }

        .next-btn {
            background: linear-gradient(135deg, var(--accent), var(--accent-hover));
            color: white;
            border: none;
            padding: 18px 32px;
            font-size: 1.1rem;
            font-weight: 700;
            border-radius: 12px;
            cursor: pointer;
            transition: all 0.3s ease;
            width: 100%;
            box-shadow: 0 4px 15px rgba(139, 92, 246, 0.4);
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .next-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(139, 92, 246, 0.6);
        }

        .next-btn:disabled {
            background: #27272a;
            color: #52525b;
            cursor: not-allowed;
            transform: none;
            box-shadow: none;
        }

        .footer {
            margin-top: 30px;
            font-size: 0.85rem;
            color: #52525b;
        }
        
        .footer a {
            color: #71717a;
            text-decoration: none;
            transition: color 0.2s;
        }
        .footer a:hover { color: var(--accent); }

        .countdown {
            margin-bottom: 20px;
            font-size: 0.9rem;
            color: #ef4444; 
            min-height: 20px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="status-badge">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"></polyline></svg>
            Bypass Complete
        </div>
        <h1 class="title">Ready to Proceed</h1>
        <p class="subtitle">Your link has been successfully bypassed. Click the button below to access your destination.</p>

        <div id="countdown" class="countdown"></div>
        <button id="nextBtn" class="next-btn">Continue to Destination →</button>

        <div class="footer">
            Powered by <a href="https://bypass.tools" target="_blank">BypassTools</a>
        </div>
    </div>
</body>
</html>`;

                const script = document.createElement('script');
                script.textContent = 'window.ock=true;';
                document.head.appendChild(script);

                (function waitForOck() {
                    console.log('[BypassTools] waitForOck (secure block) called', {
                        ock: unsafeWindow.ock,
                        dest: unsafeWindow.dest,
                        search: location.search
                    });

                    // Fallback: if ock is never set, force it true so we can proceed
                    if (unsafeWindow.ock === undefined) {
                        console.warn('[BypassTools] ock undefined (secure block) – forcing ready');
                        unsafeWindow.ock = true;
                    }
                    if (unsafeWindow.ock === true) {
                        const dest = unsafeWindow.dest || '';
                        const countdownEl = document.getElementById('countdown');
                        const btn = document.getElementById('nextBtn');

                        // Resolve destination even if the global was not set for any reason
                        const qp = new URLSearchParams(location.search);
                        const refParamRaw = qp.get('referrer') || qp.get('redirect');
                        const safeDecode = (v) => {
                            try {
                                return decodeURIComponent(v);
                            } catch {
                                return v;
                            }
                        };
                        const decodedRef = refParamRaw ? safeDecode(refParamRaw) : '';
                        const doubleDecodedRef = refParamRaw ? safeDecode(decodedRef) : '';
                        const resolvedDest = (dest || doubleDecodedRef || decodedRef || '').trim();

                        if (resolvedDest) {
                            unsafeWindow.dest = resolvedDest;
                            btn.setAttribute('data-destination', resolvedDest);
                            console.log('[BypassTools] Destination resolved (secure block):', resolvedDest);
                        } else {
                            console.error('[BypassTools] Destination missing; disabling continue button');
                            countdownEl.style.display = 'none';
                            btn.disabled = true;
                            btn.textContent = 'Destination unavailable';
                            return;
                        }

                        function hasHash(url) {
                            try {
                                return url.includes("hash=")
                            } catch {
                                return false;
                            }
                        }

                        if (hasHash(resolvedDest)) {
                            countdownEl.style.color = '#ff4d4d';
                            countdownEl.style.fontWeight = 'bold';
                            let time = 6;
                            countdownEl.textContent = `YOU HAVE EXACTLY ${time} SECONDS TO CLICK THE BUTTON BEFORE YOUR HASH EXPIRES`;
                            const interval = setInterval(() => {
                                time--;
                                if (time > 0) {
                                    countdownEl.textContent = `YOU HAVE EXACTLY ${time} SECONDS TO CLICK THE BUTTON BEFORE YOUR HASH EXPIRES`;
                                } else {
                                    countdownEl.textContent = `HASH EXPIRED! RESTARTING BYPASS PROCESS...`;
                                    countdownEl.style.color = '#ff4d4d';
                                    countdownEl.style.fontWeight = '';
                                    btn.disabled = true;
                                    clearInterval(interval);
                                    setTimeout(() => {
                                        // Get the truly original URL that started the bypass process
                                        let retryUrl = null;

                                        // Priority 1: Early capture (most reliable)
                                        if (unsafeWindow.easxEarlyCapture && !unsafeWindow.easxEarlyCapture.includes('favicon.ico')) {
                                            retryUrl = unsafeWindow.easxEarlyCapture;
                                        }

                                        // Priority 2: SessionStorage early capture
                                        if (!retryUrl) {
                                            try {
                                                const earlyCapture = sessionStorage.getItem('easx_early_capture');
                                                if (earlyCapture && !earlyCapture.includes('favicon.ico')) {
                                                    retryUrl = earlyCapture;
                                                }
                                            } catch (e) { }
                                        }

                                        // Priority 3: SessionStorage original URL
                                        if (!retryUrl) {
                                            try {
                                                const storedOriginal = sessionStorage.getItem('easx_original_url');
                                                if (storedOriginal && !storedOriginal.includes('favicon.ico')) {
                                                    retryUrl = storedOriginal;
                                                }
                                            } catch (e) { }
                                        }

                                        // Priority 4: unsafeWindow stored URLs
                                        if (!retryUrl) {
                                            if (unsafeWindow.easxOriginalUrl && !unsafeWindow.easxOriginalUrl.includes('favicon.ico')) {
                                                retryUrl = unsafeWindow.easxOriginalUrl;
                                            } else if (unsafeWindow.easxStoredOriginal && !unsafeWindow.easxStoredOriginal.includes('favicon.ico')) {
                                                retryUrl = unsafeWindow.easxStoredOriginal;
                                            }
                                        }

                                        // Priority 5: Construct from hostname
                                        if (!retryUrl) {
                                            const hostname = location.hostname;
                                            if (hostname.includes('linkvertise')) {
                                                retryUrl = `https://${hostname}/`;
                                            } else {
                                                retryUrl = `https://${hostname}/`;
                                            }
                                        }

                                        // Final fallback
                                        if (!retryUrl) {
                                            retryUrl = 'https://linkvertise.com/';
                                        }

                                        // Ensure we have a valid URL
                                        try {
                                            new URL(retryUrl);
                                        } catch (e) {
                                            retryUrl = 'https://linkvertise.com/';
                                        }

                                        console.log('[BypassTools] Hash expired, retrying with URL:', retryUrl);
                                        location.replace(retryUrl);
                                    }, 3500);
                                }
                            }, 1000);
                        } else {
                            countdownEl.style.display = 'none';
                        }

                        btn.addEventListener('click', () => {
                            if (btn.disabled) {
                                console.warn('[BypassTools] Continue clicked while disabled');
                                return;
                            }
                            const target = unsafeWindow.dest;
                            if (target) {
                                console.log('[BypassTools] Navigating to destination (secure block):', target);
                                window.location.assign(target);
                            } else {
                                console.error('[BypassTools] No destination on click (secure block)');
                            }
                        });
                    } else {
                        console.log('[BypassTools] ock not ready yet (secure block), retrying...');
                        setTimeout(waitForOck, 50);
                    }
                })();

            } else {
                document.documentElement.innerHTML = `<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BypassTools - Destination Ready</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg-color: #000000;
            --card-bg: #18181b;
            --accent: #8b5cf6;
            --accent-hover: #7c3aed;
            --text-primary: #ffffff;
            --text-secondary: #a1a1aa;
        }

        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--bg-color);
            color: var(--text-primary);
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            overflow: hidden;
        }

        .container {
            background: var(--card-bg);
            border: 1px solid #27272a;
            border-radius: 20px;
            padding: 50px;
            text-align: center;
            box-shadow: 0 0 50px rgba(139, 92, 246, 0.15);
            max-width: 480px;
            width: 90%;
            animation: fadeIn 0.8s ease-out;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .status-badge {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            background: rgba(34, 197, 94, 0.1);
            color: #4ade80;
            padding: 8px 16px;
            border-radius: 20px;
            font-size: 0.9rem;
            font-weight: 600;
            margin-bottom: 25px;
            border: 1px solid rgba(34, 197, 94, 0.2);
        }

        h1.title {
            font-size: 2.2rem;
            font-weight: 800;
            margin: 0 0 15px;
            background: linear-gradient(135deg, #fff, #a78bfa);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        p.subtitle {
            color: var(--text-secondary);
            font-size: 1.05rem;
            line-height: 1.6;
            margin-bottom: 35px;
        }

        .next-btn {
            background: linear-gradient(135deg, var(--accent), var(--accent-hover));
            color: white;
            border: none;
            padding: 18px 32px;
            font-size: 1.1rem;
            font-weight: 700;
            border-radius: 12px;
            cursor: pointer;
            transition: all 0.3s ease;
            width: 100%;
            box-shadow: 0 4px 15px rgba(139, 92, 246, 0.4);
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .next-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(139, 92, 246, 0.6);
        }

        .next-btn:disabled {
            background: #27272a;
            color: #52525b;
            cursor: not-allowed;
            transform: none;
            box-shadow: none;
        }

        .footer {
            margin-top: 30px;
            font-size: 0.85rem;
            color: #52525b;
        }
        
        .footer a {
            color: #71717a;
            text-decoration: none;
            transition: color 0.2s;
        }
        .footer a:hover { color: var(--accent); }

        .countdown {
            margin-bottom: 20px;
            font-size: 0.9rem;
            color: #ef4444; 
            min-height: 20px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="status-badge">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"></polyline></svg>
            Bypass Complete
        </div>
        <h1 class="title">Ready to Proceed</h1>
        <p class="subtitle">Your link has been successfully bypassed. Click the button below to access your destination.</p>

        <div id="countdown" class="countdown"></div>
        <button id="nextBtn" class="next-btn">Continue to Destination →</button>

        <div class="footer">
            Powered by <a href="https://bypass.tools" target="_blank">BypassTools</a>
        </div>
    </div>
</body>
</html>`;

                const script = document.createElement('script');
                script.textContent = 'window.ock=true;';
                document.head.appendChild(script);

                (function waitForOck() {
                    // ... existing logic ...
                    console.log('[BypassTools] waitForOck (normal block) called', {
                        ock: unsafeWindow.ock,
                        dest: unsafeWindow.dest,
                        search: location.search
                    });

                    // Fallback: if ock is never set, force it true so we can proceed
                    if (unsafeWindow.ock === undefined) {
                        console.warn('[BypassTools] ock undefined (normal block) – forcing ready');
                        unsafeWindow.ock = true;
                    }
                    if (unsafeWindow.ock === true) {
                        const dest = unsafeWindow.dest || '';
                        const countdownEl = document.getElementById('countdown');
                        const btn = document.getElementById('nextBtn');

                        // Resolve destination even if global is unset
                        const qp = new URLSearchParams(location.search);
                        const refParamRaw = qp.get('referrer') || qp.get('redirect');
                        const safeDecode = (v) => {
                            try { return decodeURIComponent(v); } catch { return v; }
                        };
                        const decodedRef = refParamRaw ? safeDecode(refParamRaw) : '';
                        const doubleDecodedRef = refParamRaw ? safeDecode(decodedRef) : '';
                        const resolvedDest = (dest || doubleDecodedRef || decodedRef || '').trim();

                        if (resolvedDest) {
                            unsafeWindow.dest = resolvedDest;
                            btn.setAttribute('data-destination', resolvedDest);
                            console.log('[BypassTools] Destination resolved (normal block):', resolvedDest);
                        } else {
                            console.error('[BypassTools] Destination missing; disabling continue button');
                            countdownEl.style.display = 'none';
                            btn.disabled = true;
                            btn.textContent = 'Destination unavailable';
                            return;
                        }

                        function hasHash(url) { try { return url.includes("hash=") } catch { return false; } }

                        if (hasHash(resolvedDest)) {
                            countdownEl.style.color = '#ef4444';
                            // ... existing countdown logic ...
                            let time = 4;
                            countdownEl.textContent = `LINK EXPIRES IN ${time} SECONDS`;
                            const interval = setInterval(() => {
                                time--;
                                if (time > 0) {
                                    countdownEl.textContent = `LINK EXPIRES IN ${time} SECONDS`;
                                } else {
                                    countdownEl.textContent = `HASH EXPIRED! RESTARTING...`;
                                    btn.disabled = true;
                                    clearInterval(interval);
                                    setTimeout(() => {
                                        // ... existing retry logic ...
                                        location.reload(); // Simple reload fallback for now
                                    }, 2000);
                                }
                            }, 1000);
                        } else {
                            countdownEl.style.display = 'none';
                        }
                        btn.addEventListener('click', () => {
                            if (btn.disabled) {
                                console.warn('[BypassTools] Continue clicked while disabled');
                                return;
                            }
                            const target = unsafeWindow.dest;
                            if (target) {
                                console.log('[BypassTools] Navigating to destination (normal block):', target);
                                window.location.assign(target);
                            } else {
                                console.error('[BypassTools] No destination on click (normal block)');
                            }
                        });
                    } else {
                        console.log('[BypassTools] ock not ready yet (normal block), retrying...');
                        setTimeout(waitForOck, 50);
                    }
                })();

            }
            return;
        }

        if (location.href.includes(`${CONFIG.apiBase}/wait`)) {
            // We're on the Bypass Tool wait page - let it handle the waiting
            notyf.information(`Bypass Tool processing your link...`);
            return;
        }

        if (location.hostname === 'bypass.tools' && location.pathname === "/bypass") {
            notyf.information("BypassTools is bypassing this link...");

            // Add retry button listener for bypass failures
            let retryAttempts = 0;
            const maxRetries = 3;

            const attemptBypass = async () => {
                const success = await findHref();
                if (success) {
                    notyf.success(`Redirecting to destination...`);
                    const redirect = p.get('url');
                    const host = new URL(redirect).hostname;
                    await sleep(2000);
                    const bypassUrl = `https://${host}/favicon.ico?referrer=${encodeURIComponent(success)}`;
                    window.location.href = bypassUrl;
                } else {
                    Log.e('Failed to find link');
                    retryAttempts++;

                    if (retryAttempts < maxRetries) {
                        notyf.error(`Bypass failed. Attempt ${retryAttempts}/${maxRetries}. Setting up retry...`);

                        // Set up retry button listener
                        const setupRetryListener = () => {
                            const retryBtn = findRetryButton();
                            if (retryBtn) {
                                Log.i('Retry button found, setting up listener');

                                // Replace any existing onclick handlers
                                retryBtn.onclick = null;

                                retryBtn.addEventListener('click', async (e) => {
                                    e.preventDefault();
                                    e.stopPropagation();

                                    // Call the global retry function
                                    unsafeWindow.easxRetry();
                                });
                                return true;
                            }
                            return false;
                        };

                        // Try to find retry button immediately
                        if (!setupRetryListener()) {
                            // If not found, set up observer to watch for it
                            const observer = new MutationObserver((mutations) => {
                                if (setupRetryListener()) {
                                    observer.disconnect();
                                }
                            });
                            observer.observe(document.body, {
                                childList: true,
                                subtree: true
                            });

                            // Stop observing after 30 seconds
                            setTimeout(() => observer.disconnect(), 30000);
                        }

                    } else {
                        notyf.error(`Bypass failed after ${maxRetries} attempts. Please check the URL and try again.`);
                    }
                }
            };

            await attemptBypass();
            return;
        }

        if (location.href.includes(`${CONFIG.apiBase}/?url=`) && !location.href.includes('example')) {
            await sleep(1000);
            const info = document.getElementById('information');
            if (info) info.click();
            notyf.information(`BypassTools redirecting in ${CONFIG.wait}s...`);
            let timeLeft = CONFIG.wait;
            const h1 = document.querySelector('h1');
            if (h1) {
                h1.textContent = `BypassTools redirecting in ${timeLeft}s...`;
                h1.style.color = '#1E88E5';
                const countdown = setInterval(() => {
                    timeLeft--;
                    if (timeLeft <= 0) {
                        clearInterval(countdown);
                        h1.textContent = `Redirecting now...`;
                    } else {
                        h1.textContent = `BypassTools redirecting in ${timeLeft}s...`;
                    }
                }, 1000);
            }
            await sleep(CONFIG.wait * 1000);
            const u = new URLSearchParams(location.search).get('url');
            // Redirect to Bypass Tool wait page with the specified wait time
            window.location.href = `https://${CONFIG.site}${encodeURIComponent(u)}&wait=${CONFIG.wait}`;
            return;
        }

        if (CONFIG.SecureMode) {
            (function () {
                // Store original URL for retry functionality
                if (!unsafeWindow.easxOriginalUrl) {
                    unsafeWindow.easxOriginalUrl = location.href;
                    unsafeWindow.easxStoredOriginal = location.href;
                    sessionStorage.setItem('easx_original_url', location.href);
                }

                if (document.getElementById('easx-overlay')) return;

                // Use shared Unified Overlay styles (already injected by createUnifiedOverlay or we inject specific for this)
                // We'll modify the createUnifiedOverlay style injection to be global or just reuse widely
                // But here we rely on the specific styles for this modal

                const makeStyle = () => {
                    const s = document.createElement('style');
                    s.id = 'easx-overlay-style';
                    s.textContent = `
              @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap');
              
              :root {
                --bg-color: #000000;
                --card-bg: #18181b;
                --accent: #8b5cf6;
                --border: #3f3f46;
                --text-primary: #ffffff;
                --text-secondary: #a1a1aa;
              }

              #easx-overlay {
                position: fixed !important; inset: 0 !important; z-index: 2147483647 !important;
                display: flex !important; align-items: center !important; justify-content: center !important;
                background: rgba(0, 0, 0, 0.9) !important; backdrop-filter: blur(10px) !important;
                font-family: 'Inter', sans-serif !important;
              }

              #easx-panel {
                background: var(--card-bg) !important; color: var(--text-primary) !important;
                padding: 40px !important; border-radius: 20px !important;
                max-width: 480px !important; width: 90% !important; text-align: center !important;
                border: 1px solid #27272a !important;
                box-shadow: 0 0 60px rgba(139, 92, 246, 0.2) !important;
                animation: bt-fade-in 0.5s ease-out !important;
              }

              @keyframes bt-fade-in { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: translateY(0); } }

              #easx-panel h2 {
                color: var(--text-primary) !important; margin: 0 0 15px !important;
                font-size: 2rem !important; font-weight: 800 !important;
                background: linear-gradient(135deg, #fff, #a78bfa) !important;
                -webkit-background-clip: text !important; -webkit-text-fill-color: transparent !important;
              }

              #easx-panel p {
                color: var(--text-secondary) !important; margin: 0 0 30px !important;
                font-size: 1.05rem !important; line-height: 1.6 !important;
              }

              .easx-button-group {
                display: flex !important; gap: 15px !important; flex-direction: column !important;
              }

              .easx-btn {
                padding: 16px !important; border-radius: 12px !important; border: none !important;
                cursor: pointer !important; font-weight: 700 !important; font-size: 1.1rem !important;
                transition: all 0.2s ease !important; width: 100% !important;
                font-family: 'Inter', sans-serif !important; text-transform: uppercase !important; letter-spacing: 0.5px !important;
              }

              #easx-yes {
                background: linear-gradient(135deg, #8b5cf6, #7c3aed) !important; 
                color: #fff !important;
                box-shadow: 0 4px 15px rgba(139, 92, 246, 0.4) !important;
              }
              #easx-yes:hover {
                background: linear-gradient(135deg, #a78bfa, #8b5cf6) !important;
                box-shadow: 0 8px 25px rgba(139, 92, 246, 0.6) !important;
                transform: translateY(-2px) !important;
              }

              #easx-no {
                background: transparent !important; color: #71717a !important;
                border: 1px solid #3f3f46 !important;
              }
              #easx-no:hover {
                background: #27272a !important; color: #e4e4e7 !important;
              }
              
              .easx-footer {
                margin-top: 25px !important; font-size: 0.75rem !important;
                color: #52525b !important;
              }
              .easx-footer a { color: var(--text-secondary) !important; text-decoration: none !important; }
              .easx-footer a:hover { color: var(--accent) !important; }
            `;
                    return s;
                };

                const create = () => {
                    if (document.getElementById('easx-overlay')) return;
                    if (!document.getElementById('easx-overlay-style')) document.head.appendChild(makeStyle());

                    const root = document.createElement('div');
                    root.id = 'easx-overlay';

                    const panel = document.createElement('div');
                    panel.id = 'easx-panel';
                    panel.innerHTML = `
              <h2>BypassTools</h2>
              <p>Ad-link detected. Proceed with secure bypass?</p>
              <div class="easx-button-group">
                <button id="easx-yes" class="easx-btn">
                  Start Bypass →
                </button>
                <button id="easx-no" class="easx-btn">
                  Cancel
                </button>
              </div>
              <div class="easx-footer">
                Powered by <a href="https://bypass.tools" target="_blank">BypassTools</a>
              </div>
            `;

                    root.appendChild(panel);
                    document.body.appendChild(root);
                    document.documentElement.style.overflow = 'hidden';

                    const cleanup = () => {
                        document.documentElement.style.overflow = '';
                        document.getElementById('easx-overlay')?.remove();
                    };

                    const yes = document.getElementById('easx-yes');
                    const no = document.getElementById('easx-no');

                    yes.onclick = () => {
                        cleanup();
                        const easLolUrl = `https://${CONFIG.site}${encodeURIComponent(location.href)}&wait=${CONFIG.wait}`;
                        location.assign(easLolUrl);
                    };

                    no.onclick = cleanup;
                };

                setTimeout(create, 1000);
            })();
        } else {
            // Store original URL for retry functionality
            if (!unsafeWindow.easxOriginalUrl) {
                unsafeWindow.easxOriginalUrl = location.href;
                unsafeWindow.easxStoredOriginal = location.href;
                sessionStorage.setItem('easx_original_url', location.href);
            }

            document.documentElement.innerHTML = `<h2>BypassTools - Advanced Bypass System</h2>`;
            const bypassUrl = `${CONFIG.apiBase}/wait?url=${encodeURIComponent(location.href)}&wait=${CONFIG.wait}`;
            window.location.assign(bypassUrl);
        }
    }

    (async () => {
        try {
            Log.i('BypassTools initializing...');
            await main();
        } catch (e) {
            Log.e(`Init failed: ${e.message}`);
            try { notyf.error('BypassTools Error occurred. Please contact support.'); } catch (err) { }
            try { btHandleFatalError(e, 'init'); } catch (err) { }
        }
    })();
})();